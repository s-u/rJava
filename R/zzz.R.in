# this should no longer be needed as an autoconf-edited file
.onLoad <- function(libname, pkgname) {
   library.dynam("rJava", pkgname, libname)
   # pass on to the system-independent part
   .jfirst(libname, pkgname)
}
